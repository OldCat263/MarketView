# V2.0.1 Hotfix — 代码漏洞修复（7 项）

> **版本**: V2.0.1-hotfix.1
> **优先级**: 4×P0 + 2×P1 + 1×P2
> **设计者**: 设计师
> **审批员**: 已复审通过 ✅
> **状态**: ✅ 已封版

---

## 一、7 个漏洞速览

| Bug | 级别 | 文件 | 行数 | 描述 |
|-----|------|------|------|------|
| BUG1 | **P0** | `main.py` | +8 | stock/etf 代码前缀缺失 → 批量排行全空 |
| BUG2 | **P0** | `main.py` | 同 BUG1 | 启动预计算同样失效 |
| BUG3 | P1 | `main.py` | +2 | index dict 空跑 |
| BUG4 | P1 | `main.py` | 同 BUG3 | predict_batch index 无结果 |
| BUG5 | P2 | `scorer.py` | +1 | _kline_cache 格式缺 indicators |
| BUG6 | P1 | `main.py` | +1 | crypto async 协程对象写入缓存 |
| BUG7 | **P0** | `main.py` | +3 | index 预计算 `str.get()` → AttributeError |

---

## 二、改动清单

| # | 文件 | 改动 | 行数 |
|---|------|------|------|
| A | `backend/main.py` | `_CODE_PREFIX` 映射 + `predict_batch` 前缀补全 + `_precompute_predict` 前缀补全 + index 类型守卫 + crypto await | **+14** |
| B | `backend/fetcher/scorer.py` | `_fetch_kline_cached` 缓存写缺少 MA/Boll/MACD | **+1** |
| **总计** | **2 文件** | **零新依赖** | **~15 行** |

---

## 三、验收标准

```bash
# 1. 批量排行
curl -s -X POST "http://localhost:8000/api/predict/batch/stock?pool_size=50"
sleep 8
curl -s "http://localhost:8000/api/predict/rank/stock?period=1d&limit=10"
# 应返回 >0 条

# 2. K线端点缓存完好
curl -s "http://localhost:8000/api/kline/stock/sh600519?period=1d&count=100"
# 应包含 ma/boll/macd

# 3. 指数模块不崩
curl -s "http://localhost:8000/api/predict/rank/index"
# 应返回空（index 模块 spot 结构特殊，忽略即可）
```
